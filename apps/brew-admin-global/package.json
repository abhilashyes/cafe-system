{
  "name": "brew-admin-global",
  "version": "0.1.0",
  "description": "Head-office global admin dashboard — org/store config, catalog, RBAC, loyalty, procurement, analytics, privacy. React + TS (Vite).",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --port 5174",
    "build": "tsc -b && vite build",
    "lint": "tsc --noEmit",
    "preview": "vite preview",
    "test": "echo \"(admin-global) no unit tests yet\" && exit 0"
  },
  "dependencies": {
    "@brew/contracts": "workspace:*",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.28.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.18",
    "@types/react-dom": "^18.3.5",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.7.2",
    "vite": "^6.0.7"
  }
}
