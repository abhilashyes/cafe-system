{
  "name": "brew-pos-web",
  "version": "0.1.0",
  "description": "In-store POS + store admin + Kitchen Display Screen (KDS). React + TypeScript (Vite).",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "lint": "tsc --noEmit",
    "preview": "vite preview",
    "test": "echo \"(pos-web) no unit tests yet\" && exit 0"
  },
  "dependencies": {
    "@brew/contracts": "workspace:*",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.28.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.18",
    "@types/react-dom": "^18.3.5",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.7.2",
    "vite": "^6.0.7"
  }
}
